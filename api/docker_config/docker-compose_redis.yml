version: "3.1"
services:
  redis:
    image: "redis"
    restart: always
    container_name: "redis"
    ports:
      - 15001:6379
    volumes:
      - /Users/mew/Docker/redis:/data
    command: ["redis-server", "--requirepass", "123456"]
